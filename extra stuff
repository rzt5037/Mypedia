<% if user_is_authorized_for_wikis? %>
<% end %>

<% @wiki.posts.each do |post| %>
  <div class="media">
    <%= render partial: 'votes/voting', locals: {post: post} %>
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to post.title, wiki_post_path(@wiki, post) %>
      </h4>
      <small>
        submitted <%= time_ago_in_words(post.created_at) %> ago by <%= post.user.name %> <br>
        <%= post.comments.count %> Comments
      </small>
    </div>
  </div>
<% end %>

<% if user_is_authorized_for_wikis? %>
<% end %>

<% if current_user %>
  <div class="col-md-4">
    <%= link_to "New Post", new_wiki_post_path(@wiki), class: 'btn btn-success' %>
  </div>
<% end %>
